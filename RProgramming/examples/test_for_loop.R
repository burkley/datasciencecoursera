test_for_loop <- function() {
        for(i in 1:10) {
                print(i)
        }
}

test_for_loop2 <- function() {
        files <- c("file1", "file2", "file3")
        for(f in files) {
                print(f)
        }
}
